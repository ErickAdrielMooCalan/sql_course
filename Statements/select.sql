--Selection from users table and show all items
SELECT * FROM  users;

--Selection the column called "first_name" and show all items from that column
SELECT first_name FROM users;

--Selection the columns called "id_users" and "first_name" and show all items from those columns
SELECT id_users, first_name FROM users;